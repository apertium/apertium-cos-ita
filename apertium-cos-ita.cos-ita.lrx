<rules>

  <rule>
    <match lemma="vultà" tags="vblex.*"><select lemma="tornare" tags="vblex.*"/></match>
    <match lemma="si" tags="prn.*"/>
    <match lemma="ne" tags="prn.*"/>
  </rule>
</rules>
